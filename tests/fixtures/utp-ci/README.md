# UTP CI fixtures

Synthetic files used by `tests/run-utp-tests-contract.sh` are generated inline in that script.

Real CI artifacts for regression reviews are stored under repository `_temp/` (gitignored) when downloaded locally, e.g. `_temp/<run-id>-artifacts-full/`.
